# تقرير فحص الإنتاج — Weaver
تاريخ الفحص: 2026-08-09 (فحص قراءة فقط، بدون أي تعديل على الكود)

النسختان المفحوصتان:
- Contabo: http://194.163.155.52
- Lovable: https://buildbuddy-ai-55.lovable.app

---

## 1) حرِج — نسخة Lovable معطّلة وظيفياً
`GET /api/public/health` تُرجع:

```json
{"ok":false,"db":false,"dbError":"DATABASE_URL is not configured","missingEnv":["DATABASE_URL","WEAVER_WORKER_TOKEN"]}
```

الأثر: لا قاعدة بيانات، لا مشاريع، لا عامل خلفي. كل ما يعتمد على DB يفشل.
الموقع: قراءة المتغيرات في `src/lib/db.server.ts` / `src/lib/health.functions.ts`.
الإصلاح: إما ضبط `DATABASE_URL` و`WEAVER_WORKER_TOKEN` في بيئة Lovable، أو اعتماد Contabo كنسخة إنتاج وحيدة وإيقاف الأخرى.

## 2) حرِج — المواقع المنشورة تُرجع خطأ خادم على Lovable
- `http://194.163.155.52/s/coinex` → **404** (سلوك صحيح: المشروع غير موجود في هذه القاعدة)
- `https://buildbuddy-ai-55.lovable.app/s/coinex` → **500** (خطأ خادم بدل صفحة 404)

سببان مترابطان:
1. **تباين قواعد البيانات**: كل نسخة تقرأ قاعدة مختلفة، فالمشروع المنشور على واحدة غير مرئي على الأخرى.
2. **عدم معالجة فشل الاتصال**: `src/lib/serve-site.server.ts` + `src/routes/s/$.ts` لا يلتقطان استثناء الاتصال بالقاعدة ويحوّلانه إلى 404/503 مفهومة.

## 3) عالٍ — لا TLS ولا رؤوس أمان على Contabo
رؤوس استجابة `http://194.163.155.52/`:

```
Server: nginx/1.24.0 (Ubuntu)
Content-Type: text/html; charset=utf-8
```

الناقص: `HTTPS` كلياً (الرمز السري يُرسل بنص واضح)، `Strict-Transport-Security`، `Content-Security-Policy`، `X-Content-Type-Options`، `X-Frame-Options`/`frame-ancestors`، `Referrer-Policy`. كذلك `Server` يكشف إصدار nginx.
الموقع: إعداد nginx على الخادم (`deploy/nginx*`).

## 4) متوسط — كل المسارات المحمية تُرجع 200 لغير المسجَّل
`/app` `/settings` `/monitor` `/connectors` `/platform` `/worker` `/health` كلها **200** بدون جلسة.
الحماية عميلية فقط (بوابة `_authenticated` على المتصفح). هذا مقبول بصرياً لأن البيانات تأتي من server functions محميّة، لكنه يعني تسريب هيكل الصفحات ووميض محتوى قبل التحويل. المطلوب: التأكد من أن كل server function خلف هذه الصفحات تتحقق من الجلسة فعلاً (وليس الواجهة فقط).

## 5) متوسط — جودة الكود: 765 مشكلة ESLint
`bunx eslint .` → **756 خطأ + 9 تحذيرات**، منها 727 قابلة للإصلاح آلياً بـ`--fix` (تنسيق غالباً). الباقي (~29) يحتاج مراجعة يدوية وقد يخفي أخطاء منطقية حقيقية.
ملاحظة إيجابية: **فحص الأنواع (tsgo --noEmit) نظيف تماماً — 0 خطأ**.

## 6) منخفض — لا أخطاء واجهة ظاهرة
فحص Playwright على `/` و`/auth` في النسختين:
- أخطاء console: **0**
- طلبات شبكة فاشلة: **0**
- `<h1>` واحد لكل صفحة، لا تمرير أفقي (`scrollWidth == clientWidth == 1280`)
- عناوين الصفحات وميتاداتا سليمة وعربية

## 7) الأداء — جيد
| المسار | Contabo | Lovable |
|---|---|---|
| `/` | 63 ms | 341 ms |
| `/auth` | 39 ms | 420 ms |

Contabo أسرع بـ 5–10 أضعاف. لا مشكلة أداء حالياً.

---

## أولويات الإصلاح المقترحة
1. حسم نسخة إنتاج واحدة (Contabo) وإيقاف/إصلاح متغيرات Lovable — يزيل البندين 1 و2.
2. تفعيل HTTPS (Let's Encrypt) + رؤوس الأمان على nginx — البند 3.
3. تحويل 500 إلى 404/503 في مسار `/s/$` — البند 2.
4. تشغيل `eslint --fix` ثم مراجعة المتبقي يدوياً — البند 5.
