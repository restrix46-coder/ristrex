services:
  app:
    build:
      context: ../
      dockerfile: deploy/Dockerfile
      args:
        VITE_SUPABASE_URL: ${SUPABASE_URL}
        VITE_SUPABASE_PUBLISHABLE_KEY: ${SUPABASE_PUBLISHABLE_KEY}
    container_name: weaver-app
    restart: unless-stopped
    env_file: .env
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=3000
      - WEAVER_BUILD_TARGET=node
      - RUNTIME_URL=http://runtime:4100
    volumes:
      - ../backups:/backups:ro
    # حجز موارد للمنصة نفسها حتى لا يخنقها بناء أي مشروع.
    cpus: ${APP_CPUS:-2}
    mem_limit: ${APP_MEMORY:-2g}
    ports:
      - "127.0.0.1:${WEAVER_APP_PORT:-3010}:3000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:

      - weaver
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          node -e "fetch('http://127.0.0.1:3000/api/public/live').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 60s
    depends_on:
      db:
        condition: service_healthy
      runtime:
        condition: service_healthy

  runtime:
    build:
      context: ./runtime
      dockerfile: Dockerfile
    container_name: weaver-runtime
    restart: unless-stopped
    env_file: .env
    environment:
      - RUNTIME_PORT=4100
      - RUNTIME_ROOT=/workspaces
      - RUNTIME_PORT_BASE=${RUNTIME_PORT_BASE:-5200}
      - RUNTIME_HEAVY_CONCURRENCY=${RUNTIME_HEAVY_CONCURRENCY:-1}
      - RUNTIME_MAX_SERVERS=${RUNTIME_MAX_SERVERS:-3}
      - RUNTIME_IDLE_STOP_MS=${RUNTIME_IDLE_STOP_MS:-1200000}
      - RUNTIME_WORKSPACE_TTL_MS=${RUNTIME_WORKSPACE_TTL_MS:-1209600000}
      - RUNTIME_NPM_CACHE=/workspaces/.npm-cache
    volumes:
      - runtime-workspaces:/workspaces
    networks:
      - weaver
    cpus: ${RUNTIME_CPUS:-2}
    mem_limit: ${RUNTIME_MEMORY:-2g}
    pids_limit: 512
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          node -e "fetch('http://127.0.0.1:4100/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"
      interval: 20s
      timeout: 10s
      retries: 5
      start_period: 20s

  worker:
    build:
      context: ./worker
      dockerfile: Dockerfile
    container_name: weaver-worker
    restart: unless-stopped
    env_file: .env
    environment:
      - WORKER_API_URL=http://app:3000
      - WORKER_IDLE_INTERVAL_MS=${WORKER_IDLE_INTERVAL_MS:-5000}
      - WORKER_CONCURRENCY=${WORKER_CONCURRENCY:-1}
      - MESSAGE_INTEGRITY_INTERVAL_MS=${MESSAGE_INTEGRITY_INTERVAL_MS:-900000}
    networks:
      - weaver
    cpus: ${WORKER_CPUS:-1}
    mem_limit: ${WORKER_MEMORY:-1g}
    healthcheck:
      # يتحقق من أن الـ worker حيّ عبر ملف اللمسة — مضمون ما دام العملية تعمل بشكل صحيح.
      # الملف /tmp/worker-alive يُحدَّث من داخل Worker كل دورة عمل ناجحة.
      test:
        - CMD-SHELL
        - >-
          node -e "const fs=require('fs');const f='/tmp/worker-alive';const age=Date.now()-fs.statSync(f).mtimeMs;process.exit(!fs.existsSync(f)||age>300000?1:0)" 2>/dev/null || exit 1
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    depends_on:
      app:
        condition: service_healthy


  db:
    image: postgres:17-alpine
    container_name: weaver-db
    restart: unless-stopped
    env_file: .env
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d:ro
    networks:
      - weaver
    mem_limit: ${DB_MEMORY:-1500m}
    shm_size: 256m
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 5

  backup:
    image: postgres:17-alpine
    container_name: weaver-backup
    restart: unless-stopped
    env_file: .env
    environment:
      - PGPASSWORD=${POSTGRES_PASSWORD}
      - BACKUP_DIR=/backups
      - BACKUP_KEEP_DAYS=${BACKUP_KEEP_DAYS:-14}
      - BACKUP_INTERVAL_SECONDS=${BACKUP_INTERVAL_SECONDS:-86400}
    volumes:
      - ./db/backup.sh:/backup.sh:ro
      - ../backups:/backups
    networks:
      - weaver
    entrypoint: ["/bin/sh", "/backup.sh"]
    healthcheck:
      test: ["CMD-SHELL", "ps aux | grep backup.sh || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    depends_on:
      db:
        condition: service_healthy

  nginx:
    image: nginx:alpine
    container_name: weaver-nginx
    restart: unless-stopped
    ports:
      - "${WEAVER_HTTP_PORT:-8081}:80"
      - "${WEAVER_HTTPS_PORT:-8443}:443"
    volumes:
      - ./nginx/init-ssl.sh:/init-ssl.sh:ro
      - certbot-data:/etc/letsencrypt
      - certbot-www:/var/www/certbot
    networks:
      - weaver
    healthcheck:
      # يتحقق من استجابة HTTP الفعلية، وليس فقط صحة الإعدادات.
      test: ["CMD-SHELL", "curl -sf http://127.0.0.1/api/public/health -o /dev/null || curl -sf http://127.0.0.1/ -o /dev/null || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    depends_on:
      app:
        condition: service_started
    command: "/bin/sh -c 'sh /init-ssl.sh && nginx -g \"daemon off;\"'"

volumes:
  pgdata:
  certbot-data:
  certbot-www:
  runtime-workspaces:

networks:
  weaver:
    driver: bridge
